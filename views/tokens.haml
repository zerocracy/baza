- empty = tokens.empty?
- if empty
  %p
    In order to make the
    != href('https://github.com/zerocracy/judges-action', 'here')
    plugin work in a GitHub Action workflow, it must have a token,
    which you can create here.

%form{method: 'post', action: iri.cut('/tokens/add')}
  %fieldset
    %input{ type: 'text', name: 'name', tabindex: 1, placeholder: 'Unique token name', size: 40, maxlength: 32, autofocus: true, required: true }
    %input{ type: 'submit', tabindex: 2, value: 'Add' }

- unless empty
  %p
    Full list of tokens:
  %table
    %colgroup
      %col
      %col{style: 'width: 26em;'}
      %col{style: 'width: 12em;'}
      %col{style: 'width: 4em;'}
      %col{style: 'width: 6em;'}
    %thead
      %tr
        %th
          Name
        %th
          Token
        %th
          Created
        %th.right
          Jobs
        %th
    %tbody
    - total = 0
    - max = 10
    - tokens.each(offset: offset) do |t|
      - total += 1
      - break if total > max
      %tr{class: t.active? ? '' : 'gray'}
        %td.ff
          = t.name
        %td.ff
          = t.text
        %td
          != ago(t.created)
        %td.right.ff
          = t.jobs_count
        %td
          - if t.active?
            %a{href: iri.cut('/tokens').append(t.id).append('deactivate'), onclick: "return confirm('Once you deactive this token, it will be impossible to activate it back. Are you sure you want to deactivate it?')"}
              ❌
          - else
            Inactive
  %p
    - unless offset.zero?
      %a{href: iri.del(:offset)}
        Back
    - if total > max
      %a{href: iri.over(offset: offset + max)}
        More
